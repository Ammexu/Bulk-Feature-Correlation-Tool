# Bulk-Feature-Correlation-Tool
This tool accepts a csv file and finds the spearman and pearson correlation of one of the rows (the target) with all the other rows in bulk. The output is a csv file with each row's respective pearson, spearman correlation, and number of samples.
